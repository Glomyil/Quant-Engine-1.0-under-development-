# PITFALLS.md —— 量化系统工程陷阱实录

> 记录开发过程中遇到的"数据看起来正常、代码没报错、但金融逻辑是错的"的隐蔽陷阱。
> 每条陷阱标注：发现日期、触发场景、症状、根因、修复方案。
> 跨模块共享——数据获取、清洗、回测、风控、执行都会往里记。
>
> 📌 **公开范围说明**：文中提到的「教学笔记 / 架构文档 / 交接文档 / 学习规划」等，
> 都属于本项目的**私有笔记**（只保留在作者本地，不在本仓库）。本仓库公开的内容是
> **代码 + 本文件 + README**。

---

## 目录

| # | 陷阱 | 模块 | 发现日期 | 严重程度 |
|:--|:-----|:-----|:---------|:---------|
| 1 | [ETF 产品类型混杂——"全部是 ETF K 线"的幻觉](#1-etf-产品类型混杂) | 数据清洗 | 2026-07-27 | 🔴 高 |
| 2 | [AI 架构盲区——数据格式正确掩盖了资产类别错误](#2-ai-架构盲区) | 方法论 | 2026-07-27 | 🔴 高 |
| 3 | [复权算法差异——把"复权"当单一概念，未区分加减法与比例法](#3-复权算法差异与选择) | 数据清洗 | 2026-09-08 | 🔴 高 |
| 4 | [Pipeline 执行顺序忽略性能与依赖——步骤按代码组织排而非按代价排](#4-pipeline-执行顺序忽略性能与依赖) | 方法论/架构 | 2026-09-08 | 🟡 中 |
| 5 | [df.empty 混淆"行空"与"列空"——精简写法牺牲了信号可区分性](#5-dataframeempty-混淆行空与列空) | 数据清洗/守卫 | 2026-09-08 | 🟡 中 |
| 6 | [教学代码片段默认"变量已定义"——片段不是可运行单元](#6-教学代码片段默认变量已定义) | 方法论/AI教学 | 2026-09-08 | 🟡 中 |
| 7 | [架构文档漏掉数据读取层——流水线第一环隐形](#7-架构文档漏掉数据读取层流水线第一环隐形) | 方法论/架构 | 2026-09-10 | 🟡 中 |
| 8 | [假绿测试——参数不触发被测路径（AI 冒烟块只打印不断言）](#8-假绿测试参数不触发被测路径ai-冒烟块只打印不断言) | 方法论/测试(AI产物) | 2026-09-14 | 🟡 中 |
| 9 | [契约字段只写在文档里——报告模板漏了 columns_added](#9-契约字段只写在文档里报告模板漏了-columns_added) | 方法论/契约(AI产物) | 2026-09-16 | 🟡 中 |
| 10 | [测试只喂"正常数据"——防御分支从未被执行](#10-测试只喂正常数据防御分支从未被执行) | 方法论/测试(AI产物) | 2026-09-16 | 🟡 中 |

---

## 1. ETF 产品类型混杂

**发现日期**：2026-07-27

**所属模块**：数据清洗 → ETFClassifier（Pipeline Phase 0）

**触发场景**：从搜狐 K 线 API 拉取了 1608 只"ETF"日线数据，准备写清洗模块时发现很多品种的 K 线形态异常（价格锚定在 100 附近、日内振幅极低）。

**症状**：
- `sz159001`/`sz159003`/`sz159005`：close≈100，median_amp≈0.001-0.002，57-63% 的交易日平盘
- `sh511xxx` 系列：债券/货币 ETF，median_amp < 0.01，close≈100
- 大量品种名称中含"QDII""纳斯达克""恒生"——跟踪的是海外市场

**根因**：
`etf_daily/` 目录包含的是**交易所全部挂牌 ETF**，不仅限于 A 股权益型。1608 只中：

| 资产类别 | 数量 | 占比 |
|----------|------|------|
| A 股权益型 | 1268 | 78.9% |
| QDII/跨境 | 248 | 15.4% |
| 债券型 | 51 | 3.2% |
| 黄金/商品型 | 29 | 1.8% |
| 货币型 | 12 | 0.7% |

**为什么隐蔽**：
1. 所有品种都有合法的 ETF 代码段（51xxxx/159xxx），没有 LOF（501xxx/16xxxx）或场外基金（000xxx）混入——"代码段看起来全是对的"
2. K 线数据本身格式正确——货币 ETF 有完整的 OHLC 数据，债券 ETF 有正常的日内波动——"数据没有报错"
3. 如果只看数据特征（median_amp + close），只能抓到 17/340 只非权益 ETF——"纯数据驱动分类是瞎子"
4. 1740 万行数据中，看不出任何一行"脏"——问题在 ETF 级别的元数据，不在行级别的数据质量

**如果没发现的后果**：
- 货币 ETF 进入回测 → 年化波动率 0.1% 的品种和年化 30% 的创业板 ETF 被同等对待
- QDII ETF 进入回测 → A 股 ±10% 涨跌幅限制被套用在跟踪纳斯达克的品种上（美股无涨跌停）
- 债券 ETF 进入回测 → 日内振幅 0.5% 的品种被当成"低风险股票"做多因子截面比较
- 幸存者偏差报告被稀释 → 真正因退市消失的 ETF 信号淹没在 340 只不相关品种中

**修复方案**：

1. 在 Pipeline 最上游新增 `ETFClassifier`（不继承 BaseCleaner，操作的是代码列表不是 DataFrame）
2. 双层判定：baostock 基金名称关键词（主力）→ 数据特征兜底（仅名称无关键词时启用）
3. 分类结果缓存到 `data/reference/etf_classification.json`，后续运行不联网
4. 配置 `cleaner.classifier.include_types: [equity]` 控制哪些资产类别进入清洗

**跨模块影响**：
- 回测引擎如果未来要支持 QDII ETF，需要独立的时区/汇率/涨跌幅处理分支
- 债券 ETF 不适合做动量/波动率因子，需要独立的因子计算模块
- 数据获取模块应该在拉数据时就区分资产类别，而不是拉回来再筛

**教训**：
> **"数据格式正确 ≠ 数据内容正确"**。K 线 API 不关心你拉的是股票 ETF 还是货币 ETF——它只负责返回 OHLCV。产品分类的责任在上游元数据层，不在 K 线解析层。每一层只保证自己的格式正确，但不保证语义正确。语义只能靠外部元数据校验。

**相关文档**：
- `CLEANER_ARCHITECTURE.md` §5.0 — ETFClassifier 规格
- `UPDATE.md` §8.1 — 分类关键词的长期维护风险

---

## 2. AI 架构盲区

**发现日期**：2026-07-27

**所属模块**：方法论（跨模块通用）

**严重程度**： 高

**触发场景**：使用 AI 辅助设计量化交易系统架构时，AI 接受了 1608 只 ETF 日线数据为"给定事实"，设计了一套完善的清洗流水线来处理数据质量问题（停牌、除权、时区、涨跌幅……），但从未质疑过"这些数据是否应该全部进入清洗流水线"。

**症状**：
- AI 独立完成了 11 节架构白皮书、6 个 Cleaner 规格、DuckDB+pandas 混合架构设计
- 架构中的所有检查都是"逐行"的（这一行的 OHLC 对不对？这一行的 NaN 要不要填？）
- 缺失了"逐品种"的检查（这个品种是权益 ETF 还是货币 ETF？它应该出现在这里吗？）
- 人在审查时凭金融直觉发现异常："1600 多只 ETF？好像有很多是基金而不是真正的 ETF"

**根因**：

AI 的架构设计有系统性盲区：

```
AI 擅长检查的：            AI 不会主动检查的：
┌─────────────────┐      ┌─────────────────────┐
│ 数据格式对不对    │      │ 数据本身该不该在这    │
│ 字段类型对不对    │      │ 品种是否符合使用场景  │
│ 数值范围对不对    │      │ 上游获取逻辑是否合理  │
│ 清洗步骤全不全    │      │ 分类维度需不需要引入  │
│ 接口契约严不严    │      │ 外部元数据验证身份    │
└─────────────────┘      └─────────────────────┘
     格式维度                      语义维度
   （AI 强项）                   （AI 弱点）
```

具体到本案：
1. AI 把 `etf_daily/` 目录视为"给定事实"——文件在那里，那就全部洗
2. AI 不会主动问"这 1608 只 ETF 都是 A 股权益型吗？"
3. 数据源（搜狐 K 线 API）不提供基金类型元数据——但 AI 不会主动寻找外部元数据源来交叉验证
4. 清洗器的设计哲学是"数据质量修复"——停牌就 ffill，除权就标记——没有"这个品种根本不该进清洗"的设计维度

**为什么隐蔽**：
- 这不是代码 bug——架构设计本身逻辑自洽、结构完善
- 单看每一部分都对：文件在、格式对、代码能跑、清洗能完成
- 错误发生在**需求和数据的边界**——即"数据是否符合业务假设"这个所有 AI 都不会主动验证的层面
- 人的金融直觉（"ETF 怎么可能有 1600 多只？"）是唯一的触警点

**如果没发现的后果**：
- 整个后续模块（回测引擎、因子计算、策略信号）都建立在被污染的数据基础上
- 回测绩效报告中混入了货币 ETF（年化 1-2%）和 QDII（时区错位、涨跌幅规则不同）
- 选股策略可能在债券 ETF 上产生虚假的"低波动 alpha"
- 修复成本随模块数量指数增长——清洗写完了、回测写完了、策略写完了，才发现底层数据筛选是错的

**修复方案**：
1. 承认 AI 架构设计必须经过人的"业务假设验证"——不在于代码审查，在于**输入数据的语义正确性**
2. 在数据管道的设计阶段，强制问三个问题：
   - **数据从哪来的？** → 搜狐 K 线 API，不区分基金类型
   - **数据应该包含什么？** → 交易所全部挂牌 ETF，不是只有权益型
   - **我们实际需要什么？** → A 股权益 ETF，其他类别需要排除
3. 引入外部元数据源做交叉验证（baostock 基金名称），不依赖数据本身自证身份
4. 新增 `ETFClassifier` 作为 Pipeline Phase 0——把"这个品种该不该进来"的检查**写入架构**，让它从人的直觉变成系统的内置步骤

**跨模块影响**：

这个盲区不仅限于 ETF 数据清洗。在后续模块中，同类陷阱的可能形态：

| 模块 | 可能触发场景 | AI 可能忽略的问题 |
|------|-------------|-------------------|
| 数据获取 | 拉取全市场股票日线 | ST/\*ST 股票、退市整理期股票混入正常池 |
| 回测引擎 | 按收盘价成交 | 涨跌停日无法买入/卖出，但 AI 按市价单模拟 |
| 因子计算 | 财报数据对齐 | 财报发布日期 ≠ 报告期截止日，AI 可能用错日期 |
| 风控 | 最大回撤计算 | 幸存者偏差——已清盘的产品不在回测数据中 |
| 执行 | 订单拆分 | A 股最小交易单位 100 股，AI 可能生成 37 股的订单 |

**教训**：
> **"AI 会给你一个逻辑自洽的架构，但不会验证架构的输入假设是否成立。"**
>
> 格式正确 ≠ 语义正确。人的金融领域知识负责提问："这个数据凭什么应该出现在这里？"——AI 负责在问题被提出后给出解决方案。这是"方向人定、逻辑人审、代码 AI 写"的具体体现——**人定的不仅是方向，还包括对输入数据语义边界的定义。**

**相关文档**：
- `PITFALLS.md` #1 — 本陷阱的具体案例（ETF 产品类型混杂）
- `CLEANER_ARCHITECTURE.md` §5.0 — ETFClassifier 规格
- 私有笔记：工作流（阶段三 — 人工 Review 步骤）

## 3. 复权算法差异与选择

**发现日期**：2026-09-08

**所属模块**：数据清洗 / 数据口径（复权）

**严重程度**：🔴 高

**触发场景**：设计 A 股/ETF 日线的"后复权数据"方案时，AI 只区分了"前复权/后复权/不复权"三种**口径**，把复权当作单一操作，准备直接采信厂商提供的复权 K 线；用户提问"东财的不是加减复权而不是比例复权吗"，AI 才发现复权还有**算法**这一独立维度从未校验。

**症状**：
- 同一只 510300，腾讯"后复权" hfq/raw = 0.44（倒挂），而 510050 = 1.49（正常）——同一厂商内部 ETF 复权口径自相矛盾
- 腾讯对股票的后复权正常（工行 1.65、茅台 7.02），对部分 ETF 却倒挂——"源能用"的结论不可跨品种外推
- AI 最初建议"直接换成厂商后复权价"，没有先回答"这个厂商用的是比例法还是差价法"

**根因**：

复权不是一维概念，而是二维选择，AI 只处理了一维：

```
口径维度(被处理)          算法维度(被遗漏)
前复权 qfq              比例法(乘法因子): 历史价×因子, 收益率不变 ✅
后复权 hfq    ×         差价法(加减金额): 历史价±D, 收益率被扭曲, 高分红的旧价可为负 ❌
不复权 raw
```

**为什么隐蔽**：
- 分红占价格比例小时，两法结果差异很小、不报错，短周期根本看不出
- 厂商的复权序列自身自洽（无 NaN、无异常、曲线平滑）——"看起来完全像正确数据"
- 数据源不标注自己用的算法；AI 默认"厂商复权 = 官方因子法"

**如果没发现的后果**：
- 若采信差价法序列：收益率、回撤、夏普全部被系统性扭曲（减法后 `(P2−C)/(P1−C) ≠ P2/P1`），高分红标的历史越久越失真
- 换算法后全历史结果不可比 → 前期回测结论作废，返工成本高

**修复方案**：
1. 决策显式化：**口径（前/后/不复权）× 算法（默认比例法）× 因子源** 三个选择分开做，禁止只选口径
2. 实证判定厂商算法：抽已知分红事件，检验 `复权价 = raw × factor`、除息日无跳空、非事件日收益率 ≈ raw 收益率；出现 `hfq/raw < 1` 这类倒挂 → 立即判定源不可信
3. 优先使用**官方复权因子表**（如 baostock `backAdjustFactor`），自己算 `hfq = raw × factor`，不盲信"复权 K 线"成品
4. meta 必须记录 `price_type + adjust_method + factor_source`，保证可审计、可复现
5. 自建因子时，事件表要区分**分红**与**份额折算/拆分**（两者语义不同，混在一起会算错）

**跨模块影响**：
- 收益/因子/风控指标全部依赖复权口径 → 一处错全链路错
- ETF 与股票的因子来源不同，需分别验证，不能"股票验证过就代表 ETF 没问题"

**教训**：
> **"复权不是一种操作，而是一组必须显式选择的参数：口径 × 算法 × 因子源。厂商的复权 K 线是黑盒，用之前先实证，不要默认它等于官方因子法；发现倒挂/矛盾立即弃用该源。"**

**相关文档**：
- 私有笔记：金融逻辑审查 #3（复权问题）
- 私有笔记：数据清洗教学参考（站4 统一净重）
- 本会话实证：腾讯 ETF hfq 倒挂、baostock 股票因子正常/ETF 因子为空

---

## 4. Pipeline 执行顺序忽略性能与依赖

**发现日期**：2026-09-08

**所属模块**：方法论 / 架构（数据清洗 Pipeline）

**严重程度**：🟡 中（若不改，随数据量增长升为高）

**触发场景**：AI 设计清洗 pipeline 架构时，把"代码按通用性分三组（generic→tuned→special）"直接当成了**执行顺序**。用户点出：金融大数据应先做"能直接筛选的"（用原始列），再做"需清洗/计算后才能筛选的"，最后才是"纯清洗/派生"——即**筛选先行，算力呈漏斗递减**。

**症状**：
- v1.0 架构的执行顺序只反映"谁更通用/谁代码放哪层"，不反映"谁便宜、谁依赖谁"
- 廉价且无依赖的筛选（停牌/流动性，只用 `tradestatus`/`amount` 原始列）没有被前置，反而可能排在精细复权计算之后
- 高算力派生（收益率/波动率）与廉价筛选混在同一线性列表里，顺序由"组织便利"决定 → 每次实验全量重算、内存压力大，随数据量增长形成技术债
- 环节间依赖未标注（如"极端候选需排除除权日"隐含依赖 adjust 先跑）→ 存在错序产生静默错误的风险

**根因**：

AI 结构性地缺少"**计算代价 × 数据缩减 × 依赖**"的排序思维：

```
AI 本能按……排列步骤：        正确应按……排列步骤：
代码职责/通用性分类      ←?→   计算代价(便宜先行)
模块结构美观                    数据缩减量(先缩池)
                                依赖关系(被依赖者先跑)
```

**为什么隐蔽**：
- 当前数据只有 1–2GB、向量化后"怎么排都能跑"，性能问题不报错、看不见
- 问题以技术债形式延迟暴露：全量反复实验时每次重算、内存上涨、加 DuckDB/更大池子后无法伸缩
- 依赖错序往往不崩溃，只是结果微妙地错（用未清洗数据算出的指标去筛选）

**如果没发现的后果**：
- 每次迭代全量重算 → 研究节奏被拖垮
- 后续随意插入新环节 → 顺序越来越乱、不可维护
- 依赖错序 → 静默错误数据进入回测

**修复方案**：
1. **执行顺序 = 代价与依赖轴**，与"通用性"轴解耦（通用性只决定参数来源与代码层，是标签不是顺序）：
   ```
   S0 硬剔除(数据层, 清单级, 区间外/无窗口) → P0 最便宜
   S1 廉价筛选打标(只用原始列: tradestatus/amount/close+前收) → P1
   S2 精细清洗(adjust 复权派生等, 有依赖) → P2
   S3 派生指标 + 指标级筛选(最贵最后) → P3
   ```
2. 每个环节显式标注：**依赖（消费什么）/ 产出列 / 优先级 / 算力代价**，执行器按 yml 中排好的顺序跑，yml 即默认优化序
3. **能先筛的用原始列先筛**：amount（流动性）、tradestatus（停牌）、close+前收（涨跌停超限）都不依赖复权 → 不必等清洗
4. **打标与硬剔除分离**：硬剔除只做无争议（区间外）；ST/流动性/极端波动一律打标，删除权归策略层
5. 大数据场景用 DuckDB 全库粗筛出候选池（S0/S1），pandas 只精洗候选池

**跨模块影响**：
- 适用于一切"清洗 → 算指标 → 筛选"类管道（因子预处理、回测数据准备、分钟线）
- 与 #2 同源：AI 的结构性盲区——这次在"数据流的代价与顺序"，上次在"输入数据的语义"

**教训**：
> **"步骤顺序由代价与依赖决定，不由代码组织便利决定。先问：哪些判断用原始列就能做（先行筛掉）？再问：谁依赖谁？最后才排纯清洗与派生。"**

**相关文档**：
- 私有设计文档：清洗 Pipeline 架构 v1.1（§2 两个正交轴 / §3 优化链条 / §4 依赖与优先级标注）
- 用户原话：筛选先行 → 清洗接筛选 → 纯清洗最后

---

## 5. DataFrame.empty 混淆"行空"与"列空"

**发现日期**：2026-09-08

**所属模块**：数据清洗 / 通用守卫（pandas API 语义）

**严重程度**：🟡 中

**触发场景**：编写 `base.check_not_empty` 时，AI 提议用 `df.empty` 判空表"代码更精简"；结合列式存储（parquet：schema=列、row groups=行）讨论后，发现 `empty` 的语义与"我要判的是不是没有交易日（行）"不匹配。

**症状**：
- `df.empty` 的定义：**两个轴（行或列）只要有一个长度为 0** 即为 True
- 列式存储读取的常见结果被它合并成一个信号：
  - "有 schema 无行"（查未来区间 / 该股那段未上市）→ `empty=True`
  - "缺列 / 0 列有行"（文件结构坏 / 读取方式错）→ 也可能 `empty=True`
- 若用它做守卫：0 行（**可能合法的业务状态**：未上市/整段停牌/区间外）与缺列（**永远是 bug**）会报同一句"数据为空"，无法区分

**根因**：
- API 语义（empty = 任一轴为 0）与业务语义（我要判"没有交易日 = 行数"）不匹配
- AI 追求"精简"而牺牲了**信号的可区分性**
- 列式存储天然把 **行（数据量）** 与 **列（schema）** 分成两个维度，需要两个独立守卫各管一维

**为什么隐蔽**：
- 常见场景（0 行且有列、正常读盘）下 `len(df)==0` 与 `df.empty` 等价，看不出差别
- 问题只在边界（0 列有行、需要区分"缺列 vs 无行情"）与"错误信息可行动性"上暴露
- "代码短"让人误以为"语义也准"

**如果没发现的后果**：
- 空表误报掩盖缺列真因 → 排查方向错（以为数据源没数据，其实是 schema/读取问题）
- 合法 0 行被当错误（或反之）→ 几千只批量跑时分不清"该跳过"还是"该修代码"
- 报错信息无法指导下一步行动 → 违背可审计原则

**修复方案**：
1. 行与列**分开守卫**：`len(df)==0` 只管行；`check_required_columns` 点名缺哪几列
2. 语义定死：**行空 = 业务现象**（可走 no_data/跳过分支），**列空 = 结构错误**（必须 raise）
3. 报错信息要能回答"该跳过还是该修代码"

**跨模块影响**：
- 一切读 parquet/pandas 的守卫代码
- DuckDB 同构：0 行 = `count(*)` 为 0（正常），字段写错 = 解析期报错——行可为空、列不能空
- 类似"空值语义"陷阱：`df.all()/any()`、`np.any` 对空序列也有反直觉行为，用前先确认语义

**教训**：
> **"别用'看起来精简'的 API 替代语义明确的检查——先问：这个信号我要用来区分什么？行空是业务、列空是结构，别用 df.empty 一锅烩。"**

**相关文档**：
- 私有教学笔记：base.py 教学参考（守卫部分：质检台）
- 本会话实测：`df[df["date"]>"2099-01-01"]` → 0 行有列 vs `drop(columns=["volume"])` → 有行缺列

---

## 6. 教学代码片段默认"变量已定义"——片段不是可运行单元

**发现日期**：2026-09-08

**所属模块**：方法论 / AI 辅助教学（代码片段文档）

**严重程度**：🟡 中

**触发场景**：私有教学笔记（base.py 教学参考）给出的函数片段直接引用 `REQUIRED_COLUMNS`、`pd` 等，但片段内没展示它们的定义；用户照抄进一个空文件后 VSCode 报 `REQUIRED_COLUMNS is not defined`，并一度产生"调用时定义 cols 就行 / 编辑器报错没关系"的错误理解——直到用测试文件一验，才发现模块级常量根本没定义、代码根本无法运行。

**症状**：
- 教学片段"逻辑看得懂"，但复制到空文件**一跑就 NameError**（缺常量/缺 import）
- 学习者把"名字未定义"误当成"可忽略的编辑器提示"或"由调用方在运行时提供"
- 需要靠测试文件（如 test_base.py）才暴露出"文档代码不是完整模块"

**根因**：
- AI 生成教学片段时**默认读者已有文件上下文**（imports/常量/包结构），只展示"增量代码"——片段不是可运行单元
- 未区分"解释用片段（省略上下文）"与"可运行片段（从 import 到可测试）"
- 文档没给"这段代码依赖哪些前置定义"的清单

**为什么隐蔽**：
- 片段单独看逻辑自洽；对话/文档前文刚讲过常量，让人以为"已定义"
- 对初学者尤其致命：会把"缺定义"误归为"编辑器报错没关系"，或以为变量由调用方提供

**如果没发现的后果**：
- 照抄无法运行 → 学习者怀疑自己、浪费排查时间
- 把"名字未定义"当可忽略 → 养成坏习惯，将来生产代码同样埋 NameError
- 文档与真实代码脱节 → 教学参考失去"参考"价值

**修复方案**：
1. 每个教学片段给"**前置要求**"：需要哪些 import/常量/包结构；或直接给"**完整可运行最小文件**"
2. 明确区分两种片段：`解释片段（省略上下文，标注依赖）` vs `可运行片段（含 import 与常量）`
3. 文档配套"**验收冒烟测试**"（如 test_base.py）——强制验证文档代码能真正跑起来
4. 学习者心智：空文件里任何"名字未定义"都是**真错误**，不是可忽略提示；检查顺序 = import → 常量 → 函数 → 调用

**教训**：
> **"教学代码片段默认读者已有上下文，是最隐蔽的隐性假设。文档代码必须是'可运行单元'，或明确标注前置依赖；'名字未定义'永远是错误信号，不是'调用时再定义'的借口。"**

**相关文档**：
- 私有教学笔记：base.py 教学参考（本次事故现场）
- 本会话：用户 base.py 缺 `REQUIRED_COLUMNS` 定义导致测试失败

---

## 7. 架构文档漏掉数据读取层——流水线第一环隐形

**发现日期**：2026-09-10

**所属模块**：方法论 / 架构（数据清洗 Pipeline）

**严重程度**：🟡 中

**触发场景**：写 `align.py` 时想搞清楚"`run(df, params, ctx)` 里的 `df` 到底是谁给的、parquet 是怎么变成 DataFrame 的"，翻架构文档却发现：§5 目录结构只列了 `data/cleaner/` 下的 8 个文件，§10 实现顺序是 `base → pipeline(空跑) → align → …`，**全文没有任何"数据读取模块"**。

**症状**：
- 流水线第一环（谁读文件、谁裁剪区间、谁做守卫）无处可查，只能靠推测；
- 反复追问"df 怎么作用的""是不是没写 loader 所以理解不了"——理解成本被转嫁到人身上；
- 容易产生"cleaner 会自己读文件""pipeline 会帮忙读文件"的误解（实际 cleaner 只吃 DataFrame）。

**根因**：
- 架构以"**清洗环节**"为中心组织：§4.2 表里只有一行括号写着"(数据层)"，§5 目录只列 cleaner 下的文件 → **数据层被写成了形容词，而不是一个文件**；
- 下载器(fetcher)已经完成了"存储"那一半，给人"数据层已经有人管"的错觉；
- 手工 df 就能单测每个环节 → 缺失的读取层**在日常开发中不产生任何报错**，直到要端到端跑才暴露。

**为什么隐蔽**：
- 代码能跑、单测能过、文档"看起来完整"（环节表、依赖图、yml 示例都有）；
- 缺的是契约链的**第一格**，而人习惯从中间开始读文档，很难发现"入口"没写。

**如果没发现的后果**：
- 契约断层：loader 的输出边界（要不要排序/去重/类型修正）无人定义 → 容易和 align 抢活或互相推诿；
- 每个新会话/新成员都要重新问一遍同一个问题（本次就是）；
- 端到端首次联调时集中爆雷（读进来的 df 列不全 / 区间没裁 / 空表没分支）。

**修复方案**：
1. 架构升 v1.2：§5 目录结构补 `data/loader.py`；§10 实现顺序把它插在 `base.py` 之后、`align.py` 之前；**新增 §5.1"线性数据流"**（parquet → loader → pipeline → 环节 → 写回 一张图）；
2. 写死 loader 的**输出契约**：列齐 + 区间内 + 有行（行空→跳过并计数；缺列→raise），**不负责**排序/去重/类型（那是 align 的承诺）；
3. 明确 loader **不属于 cleaner**：不进 yml 的 pipeline 列表、不写 `run(df, params, ctx)` 签名。

**跨模块影响**：
- 一切 ETL/因子/回测数据管道同理：架构文档必须画出**数据的入口与出口**（读什么、怎么写、契约是什么），不能只画中间的处理环节。

**教训**：
> **"架构文档只写中间的处理环节，第一环就会隐形。"** 一条流水线必须写全三件事：**入口（谁读、读什么、怎么裁）、中间（环节与依赖）、出口（写什么、报告长什么样）**；只写中间，等于把"数据从哪来"留给读者猜。

**相关文档**：
- 私有设计文档：清洗 Pipeline 架构 v1.2（§5 目录 / §5.1 线性数据流 / §6 输出契约 / §10 实现顺序）
- 私有交接文档（本次会话：用户决定先写 loader 再回 align）

---

## 8. 假绿测试——参数不触发被测路径（AI 冒烟块只打印不断言）

**发现日期**：2026-09-14

**所属模块**：方法论 / 测试（数据层 loader 冒烟）；**AI 产物类**

**严重程度**：🟡 中

**本次揪出的 AI 错误（具体到文件与位置）**：

| # | AI 在哪里犯错 | 错的具体内容 | 造成的后果 |
| :-: | :--- | :--- | :--- |
| 1 | 私有教学笔记（loader.py 教学参考）**§5 参考实现的 `__main__` 冒烟块** | 只有 `print(...)`、**一个 `assert` 都没有**；且只调用一次 `load_one("sh.600000", "2020-01-01", "2020-12-31")`——**只覆盖主路径** | 跑出来的数字对不对全靠人肉看；边界（首尾日）与行空分支完全没覆盖 → 天生具备"假绿"体质 |
| 2 | 同一文档 **§4 验收用例**里的 `iter_frames` 那一条 | 只 `print("iter_frames 前 5 只产出:", n)`，**不断言**（文档没写"产出数 ≤ 输入数"） | 读者会默认"输入 5 只就该出 5 只"，一旦真实数据里含退市股就产生认知冲突 |
| 3 | AI 在**口头验收环节临时写的断言**（终端里跑的验收脚本，非仓库文件） | `assert n == 5`——断言 `iter_frames(codes[:5], "2020-01-01", "2020-12-31")` 必须产出 5 只 | **当场被真实数据证伪**：`sh.600005` 于 2017-02 退市、2020 年无数据 → 正确行为是跳过，实际产出 4 只。错误本质：**把"产出数 = 输入数"当成默认假设，没先确认真实数据（含退市股）的行为** |

> 这三条的共同特征：**AI 生成的"验证代码"看起来在验证，其实没有判定能力（只打印）或判定前提错误（想当然的断言）**。
> 这与 #6（教学片段不是可运行单元）同源——**AI 产物必须被"能失败"的标准复核**。

**触发场景**：给 `data/loader.py` 的 `__main__` 冒烟块"乱改参数"做实验：把调用改成
`load_one("sh.99999999", "2020-01-", "2020-26")`（**不存在的代码 + 两个非法日期字符串**），
结果脚本照常打印 `候选清单: 5471` 与 `区间内 shape: None`，看起来"完全正常"。

**症状**：
- 参数已经被改坏（非法日期 `"2020-01-"`、`"2020-26"`），却没有任何报错；
- 冒烟脚本"通过"，但主路径（读文件 → 守卫 → 时间解析 → 区间裁剪 → `reset_index`）**一行都没执行**；
- 全绿的结果与代码正确性无关——典型的**假绿（false green）**。

**根因**：
- 函数体内的**早退分支**在参数被真正使用之前就结束了调用：

```python
path = store / f"{code}.parquet"
if not path.exists():
    return None            # ← sh.99999999.parquet 不存在 → 从这里就返回了
...
t0, t1 = pd.Timestamp(start), pd.Timestamp(end)   # ← 这两行永远没被执行
```

- Python **逐句执行函数体**：`return` 之后（该路径上）的语句不会执行，
  所以 `pd.Timestamp("2020-01-")` 从未被求值；
- 测试参数选得"不触发被测路径"：用不存在的代码，只覆盖了最外围的分支。

**实验证据（同一份代码，只换"文件是否存在"）**：

```
load_one('sh.99999999', '2020-01-', '2020-26')  → None             ← 短路，日期从未解析
load_one('sh.600000',  '2020-01-', '2020-26')  → DateParseError    ← 文件存在, 立刻炸
      Unknown datetime string format, unable to parse: 2020-01-
pd.Timestamp('2020-26') → DateParseError: month must be in 1..12
```

**为什么隐蔽**：
- 输出**看起来合理**（打出了清单数和一个 `None`），没有异常、没有警告；
- 解释器/编辑器不会提示"你的参数有问题"；
- "测试通过"本身带来**错误的安全感**，让人以为主路径已被验证。

**如果没发现的后果**：
- 真正的 bug（如 `pd.Timestamp[start]` 用错下标语法、区间边界写成 `>`/`<` 漏首尾、
  `set_index` 误写成 `reset_index`）会被"绿测试"掩盖；
- 更糟的是养成"测试写了就行"的习惯 → 测试失去回归保护作用，
  以后改 loader 时无法发现破坏性改动。

**修复方案**：
1. **参数必须能走到被测代码行**：主路径用真实代码 + 合法区间
   （`load_one("sh.600000", "2020-01-01", "2020-12-31")` → 期望 `(243, 11)`）；
2. 一个冒烟块至少覆盖**三类分支**：主路径 / 边界（首尾日含入）/ 异常分支（行空→None）；
3. **先让测试红一次**再让它绿（参考私有教学笔记：align.py 教学参考 §4 的"红→绿"流程），
   确认这个测试**有能力失败**；
4. 断言要**具体**（`shape == (243, 11)`、`df["date"].min() >= t0`），不要只 `print`——
   只打印不判断 = 没有测试；
5. 养成一个反问：**"我给的这组参数，会真正执行到我想验证的那几行吗？"**
6. **断言要落在文件里**（写进 `__main__` 冒烟块或 `tests/`），不要只在终端临时跑一次——
   临时断言既不进回归，也正是"想当然"的温床（本轮 AI 的 `assert n == 5` 就是临时断言）；
7. **AI 产出的验证代码要按"能失败"标准复核**，三条任一命中就重写：
   ① 只 `print` 不断言；② 只覆盖主路径（缺边界/异常分支）；③ 断言前提未经真实数据验证（如"产出数 = 输入数"）。

**跨模块影响**：
- 任何"用手工输入验证函数"的场景：清洗环节单测、回测参数校验、下载器补跑验证；
- 同类的还有"边界参数不触发分支"：例如用正常股票去测"退市股跳过"、用有数据的区间去测"空区间"分支，
  都会得到假绿。

**教训**：
> **"永远绿的测试等于没有测试。"** 写测试先问一句：
> **这组参数会走到我想验证的那几行吗？**
> 早退分支（文件不存在 / 表为空 / 开关关闭 / 区间外）最容易把测试变成安慰剂。
>
> 对 AI 产物再补一句：**"看起来在验证" ≠ "验证过了"。** AI 很容易给出"只打印不断言""只测主路径"
> "断言前提想当然"的验证代码——它读起来像测试，实际上没有判定能力（与 #6 同源）。

**相关文档**：
- 私有教学笔记：loader.py 教学参考 §4 验收用例（已补"产出数 ≤ 输入数"的提醒）
- `data/loader.py` 的 `__main__` 冒烟块（本轮已改为覆盖三类分支）

---

## 9. 契约字段只写在文档里——报告模板漏了 columns_added

**发现日期**：2026-09-16

**所属模块**：数据清洗 / 报告与契约；**AI 产物类**

**严重程度**：🟡 中

**触发场景**：用户在 `pipeline.run_one` 里写 `rep["columns_added"] = [...]`，
为了核对，回去看 `base.new_step_report` 的实现，发现模板里只有 `name/status/metrics/warnings/errors`
—— **没有 `columns_added`**；而架构文档 §6 明确写着报告格式含 `columns_added`。

**本次揪出的 AI 错误（具体到位置）**：

| # | AI 在哪里犯错 | 错的具体内容 | 后果 |
| :-: | :--- | :--- | :--- |
| 1 | 架构文档 §6（报告格式定义） | 声明报告含 `columns_added` | 文档说"有这个字段" |
| 2 | `data/cleaner/base.py` 的 `new_step_report` 模板 | 只预置 `name/status/metrics/warnings/errors`，把 `columns_added` 留给调用方自己补，且**没有任何注释说明归属** | 两份 AI 产物不一致，契约漏了一环 |
| 3 | 两份产物之间 | 没有任何一致性检查（没有测试断言"实际键集合 == 契约键集合"） | 漏填/拼错都静默通过 |

**症状**：
- 文档定义的契约字段，代码模板里不存在 → 每个使用方都要**自己记着补**；
- Python dict 允许随时加键，所以漏填、拼错（如 `columns_addded`）**都不报错**；
- 报告少一个字段，人工看报告几乎发现不了（除非专门断言键集合）。

**根因**：
- 契约字段被定义在**两处**：架构文档 §6（报告格式）与 `base.new_step_report`（实现），两边没有对齐检查；
- 写模板时只列了"通用字段"，把执行器观测字段（`columns_added`）当成"调用方自己补"，但既没在代码里预置，也没在文档里点名归属；
- Python 的 dict 没有 schema 约束 → 没有编译期/运行期保护。

**为什么隐蔽**：
- 不报错、不告警，报告"看起来正常"；
- 只有"专门检查报告键"或"下游读取该字段"时才暴露；
- 与 #6（教学片段不是可运行单元）同源：**AI 产物的契约在传递过程中漏了一环**。

**如果没发现的后果**：
- 执行器漏填 → 白名单检查结果（哪一步加了未登记列）永远为空，等于没做契约检查；
- 拼错键名 → 报告里出现"幽灵字段"，真正要读的字段却缺失；
- 下游（汇总报告/验收脚本/可视化）要么 `KeyError`，要么静默拿到 `None`；
- 契约长期漂移：文档一份、实现一份，越走越远。

**修复方案**：
1. 把契约字段**预置进代码模板**：`new_step_report` 里直接给出 `"columns_added": []`（谁都不用记）；
2. 在 base 里用注释写清"每个键谁填"（base 预置 / 执行器观测）；
3. 加一条**契约测试**：断言 `set(new_step_report("x")) == {契约字段集合}`
   —— 让"文档即契约"变成可执行的检查（推荐，成本约 3 行）；
4. （以后）用 `TypedDict` / `dataclass` 给报告结构加类型约束，静态检查即可抓拼写；
5. 规则化：**任何"文档定义的字段/结构"，都要么在代码里预置，要么在测试里被断言。**

**跨模块影响**：
- 一切"文档定义结构、代码手工填字段"的场景：报告 dict、`cleaner_meta.json`、`stock_meta.json`、yml 的必填键；
- 通用原则：**契约必须可验证，不能只可阅读。**

**教训**：
> **"写在文档里的字段，等于没写。"** 契约字段必须在代码里预置、或在测试里被断言；
> 否则它迟早被漏填或拼错，而且是**静默**的。
>
> 这条同时是 AI 的教训：AI 能写出一致的文档和一致的代码，但**文档与代码之间的一致性**
> 没人检查时就会漏——#6（教学片段）、#7（架构缺 loader）、#9（报告字段）是同一个病：
> **AI 产物之间的接缝，必须由人（或测试）来焊。**

**相关文档**：
- `数据清洗Pipeline架构.md` §6（报告格式：steps/metrics/warnings/errors/columns_added）—— 私有笔记
- `data/cleaner/base.py`（`new_step_report` 模板）
- 私有教学笔记：base.py 教学参考（报告四件套）

---

## 10. 测试只喂"正常数据"——防御分支从未被执行

**发现日期**：2026-09-16

**所属模块**：方法论 / 测试；**AI 产物类**

**严重程度**：🟡 中

**触发场景**：升级 align 验收用例时，为"测得更精准"把测试数据里的数字**故意写成字符串**（`"2.0"`），
并加了一个坏值 `"abc"`。实测对比后发现：**同样的实现，干净数据测不出 bug，脏数据一测就露**：

```
【float 数据】  忘了 to_numeric → dtype=float64, close*2=[2.0, 4.0]      ❌ 抓不到
【字符串数据】  忘了 to_numeric → dtype=object,  close*2=['1.01.0', ...] ✅ 立刻抓到
```

**触发场景（续）**：新写的脏数据测试首次运行，就抓到 align 草稿里的两个真实 bug：
① 在环节里调用 `base.check_new_columns(df)`（该函数是**执行器**用来对比前后列的，签名 `(before_cols, after_cols)`，缺参数直接 `TypeError`）；
② `int(out["date"].dropna().sum())` 应为 `isna().sum()`——`dropna()` 把 NaT 扔掉后对 datetime 列求和会 `TypeError`。

**症状**：
- 测试全绿，但**防御性代码从未执行**（`errors="coerce"`、坏值分支、异常分支、边界）；
- 断言只比较"值"，不比较 **dtype**（`"1.0"` 与 `1.0` 的差异被掩盖）；
- 异常/守卫分支（空表、缺列、坏值）没有对应用例。

**根因**：
- 测试是"**按函数写的**"，不是"**按输入形态矩阵写的**"——函数的输入有 N 种形态，只喂了最正常的一种；
- AI 生成测试/验收代码时默认使用**干净输入**（与 #8 同源，但 #8 是"路径没走到"，本条是"**形态没覆盖**"）；
- 行覆盖率会给出虚假的安全感：`if col in out.columns` 这类分支，用干净数据也能"跑过"，但内部 `to_numeric` 的结果从未被验证。

**为什么隐蔽**：
- 干净数据下，正确实现与"忘了写 to_numeric"的实现**输出完全一样**；
- 不报错、覆盖率漂亮、CI 全绿；
- 只有数据源变化（CSV / 网页接口 / 上游吞坏值）时才暴露——而那时 bug 已在生产路径上。

**如果没发现的后果**：
- 字符串列参与计算 → `"1.0" * 2 = "1.01.0"`（**静默错误**，结果全错但不报错）；
- 坏值/缺失策略无人验证 → 某天上游塞了个 `"abc"`，整列变 object，下游指标全歪；
- 守卫（空表/缺列）没有用例 → 真坏了才发现守卫写错方向。

**修复方案**：
1. **按"输入形态矩阵"设计数据**：正常 / 字符串 / 坏值 / 缺失 / 乱序 / 重复 / 边界 / 空表 / 缺列；
2. **断言 dtype 与 isna**，不能只比值：`assert out["close"].dtype == "float64"`、`assert pd.isna(x)`；
3. **干净数据与脏数据两类都要**：干净测"正常路径"，脏测"防御路径"；
4. 每条断言都要能回答："**它执行到了我想验证的那几行吗？**"（与 #8 呼应）；
5. 测试数据本身就是规格的一部分——写进验收文档，别只存在于脑子里。

**跨模块影响**：
- 一切"读外部数据"的模块：下载器解析、CSV 导入、接口返回值、配置读取；
- 与 #8 是姊妹条：#8 管"参数走不到被测路径"，#10 管"参数形态不足以触发防御分支"。

**教训**：
> **"干净数据测正常路径，脏数据测防御路径。"**
> 覆盖率不等于验证过；**测试数据的形态，决定了你能不能看见 bug**。
> 一个只会喂正常数据的测试，等于给实现发了一张"免检通行证"。

**相关文档**：
- `tests/test_align.py`（脏数据矩阵版：字符串数字 / 坏值 / 缺失 / 守卫 / 幂等）
- PITFALLS #8（假绿测试）、#6（教学片段不可运行）——同属"AI 产物验证不足"
- 私有教学笔记：align.py 教学参考 §4

---

## 附：防再犯素材（插件可读）

```json
[
  {
    "id": "PITFALL-003",
    "type": "cognition_finance",
    "title": "复权算法维度缺失",
    "principle": "复权 = 口径(前/后/不复权) × 算法(比例/差价) × 因子源，三维都要显式选择；默认比例法+官方因子表，厂商复权K线是黑盒需实证。",
    "trigger_checks": [
      "设计复权/调整价方案时是否只提了 qfq/hfq/raw 而未问算法？",
      "是否准备直接采信某个源的复权序列而未验证 hfq=raw×factor？",
      "出现 hfq/raw<1、跨品种因子不一致时是否仍在用该源？"
    ]
  },
  {
    "id": "PITFALL-004",
    "type": "architecture_perf",
    "title": "Pipeline 顺序忽略代价与依赖",
    "principle": "执行顺序 = 筛选先行(S0/S1 用原始列) → 清洗(S2) → 派生与指标筛选(S3)；通用性只是标签不是顺序；每环节标注依赖/产出/优先级。",
    "trigger_checks": [
      "设计管道时是否按代码职责/通用性排步骤而非按算力漏斗排？",
      "是否存在'可用原始列先筛'却排在精细计算之后的环节？",
      "环节间依赖(如极端候选需在 adjust 之后)是否显式标注？",
      "硬剔除与打标是否混在一起(替策略做了删除决定)？"
    ]
  },
  {
    "id": "PITFALL-005",
    "type": "api_semantics",
    "title": "df.empty 混淆行空与列空",
    "principle": "守卫要区分维度：len(df)==0 管'没有行'(业务可空,可走no_data分支)，缺列检查管'没有字段'(结构错误必须raise点名)；df.empty 把两维合并成一句'空'，报错不可行动。",
    "trigger_checks": [
      "用 df.empty / shape 判'空表'时，是否真的不需要区分'没行'和'没列'？",
      "批量读 parquet 的守卫是否把'0行(可能合法)'与'缺列(bug)'分开处理？",
      "报错信息能否让接收者判断'该跳过还是该修代码'？"
    ]
  },
  {
    "id": "PITFALL-006",
    "type": "doc_teaching",
    "title": "教学代码片段默认变量已定义",
    "principle": "教学片段必须是'可运行单元'或明确标注前置依赖(import/常量/包结构)；区分'解释片段'与'可运行片段'并配验收测试；'名字未定义'永远是错误信号。",
    "trigger_checks": [
      "给出的代码片段是否默认读者已有 import/常量上下文？片段能独立运行吗？",
      "是否区分了'解释片段(省略上下文)'与'可运行片段(含import和常量)'？",
      "文档是否配套了能验证片段的冒烟测试？",
      "学习者问'报错没关系吧'时，是否第一时间纠正为'名字未定义=真错误'？"
    ]
  },
  {
    "id": "PITFALL-007",
    "type": "architecture_contract",
    "title": "架构文档漏掉数据读取层（流水线第一环隐形）",
    "principle": "流水线架构必须显式定义入口(谁读文件/裁剪规则/守卫)、出口(写回/报告)及其数据契约；只写中间处理环节，第一格就会隐形，理解成本与联调风险会转嫁给人和下游。",
    "trigger_checks": [
      "架构文档的目录结构与实现顺序里，有没有'数据读取'这个具体文件？",
      "数据层的输出契约(列齐/区间内/行空处理/是否负责排序去重)是否白纸黑字？",
      "数据层是否被误当成清洗环节(进了 yml 名单或写了 run 签名)？",
      "新人/新会话是否会重复问'df 从哪来'？"
    ]
  },
  {
    "id": "PITFALL-008",
    "type": "testing_false_green",
    "title": "假绿测试（参数不触发被测路径 / AI 冒烟块只打印不断言）",
    "principle": "测试必须有能力失败：参数要能走到被测代码行，断言要能判定。AI 产出的验证代码常见三个毛病——只 print 不断言、只覆盖主路径、断言前提想当然；它们读起来像测试，实际没有判定能力。",
    "ai_mistakes": [
      "私有教学笔记 loader.py 教学参考 §5 的 __main__ 冒烟块：只有 print、没有任何 assert，且只覆盖主路径",
      "同文档 §4 验收里 iter_frames 一条：只 print 产出数，未注明'产出数 ≤ 输入数（区间内无数据会被跳过）'",
      "AI 口头验收时临时断言 assert n == 5：把'产出数 = 输入数'当默认假设，被退市股 sh.600005 当场证伪（实际产出 4 只）"
    ],
    "trigger_checks": [
      "我给的这组参数，会真正执行到我想验证的那几行吗？",
      "这条测试失败过吗（先红后绿）？",
      "断言是否具体（shape/数值/边界）且写进文件，而不是只 print / 只在终端临时跑一次？",
      "是否覆盖了主路径 + 边界 + 异常三类分支？",
      "AI 给的验证代码，是否只打印不断言、只测主路径、或断言前提想当然？"
    ]
  },
  {
    "id": "PITFALL-009",
    "type": "contract_doc_code_drift",
    "title": "契约字段只写在文档里（报告模板漏了 columns_added）",
    "principle": "文档定义的字段必须在代码里预置、或在测试里被断言。只写在文档里的契约字段迟早被漏填或拼错，而 dict 会静默接受错误键名；文档与代码的一致性必须由测试焊住。",
    "ai_mistakes": [
      "架构文档 §6 声明报告格式含 columns_added，但 base.new_step_report 只预置 name/status/metrics/warnings/errors，且未注释字段归属",
      "两份产物都是 AI 生成的，中间没有任何一致性检查 → 契约在传递中漏了一环（与 #6/#7 同源）"
    ],
    "trigger_checks": [
      "文档里定义的字段/结构，在代码里是预置的，还是靠调用方手填？",
      "有没有一条测试断言『实际结构 == 契约结构』（键集合 / 必填键）？",
      "用 dict 当结构时，拼错键名会报错吗？（不会 → 需要预置或类型约束）"
    ]
  },
  {
    "id": "PITFALL-010",
    "type": "testing_input_matrix",
    "title": "测试只喂正常数据（防御分支从未被执行）",
    "principle": "干净数据测正常路径，脏数据测防御路径。测试数据要覆盖输入形态矩阵（正常/字符串/坏值/缺失/边界/异常），并断言 dtype 与 isna 而不只比值；否则 coerce/守卫/边界分支从未被验证。",
    "ai_mistakes": [
      "AI 写的 align 验收用例里数字全是 float、没有 dtype 断言 → to_numeric 那几行永远不会被真正执行",
      "AI 生成测试默认使用干净输入，不覆盖坏值/缺失/异常分支"
    ],
    "trigger_checks": [
      "测试数据的形态够不够触发防御分支（coerce / raise / 边界）？",
      "断言里有没有 dtype / isna，而不是只比值？",
      "干净数据与脏数据两类用例都有吗？"
    ]
  }
]
```

---

后续发现新陷阱时，复制此模板：

```markdown
## N. [陷阱名称]

**发现日期**：YYYY-MM-DD
**所属模块**：数据获取 / 数据清洗 / 回测引擎 / 风控 / 订单执行
**严重程度**：🔴 高 / 🟡 中 / 🟢 低
**触发场景**：
**症状**：
**根因**：
**为什么隐蔽**：
**如果没发现的后果**：
**修复方案**：
**跨模块影响**：
**教训**：
**相关文档**：
```

---

> **更新日志**
> - 2026-07-27：初始版本，收录 ETF 产品类型混杂陷阱
> - 2026-09-08：收录 #3 复权算法差异（认知类）、#4 Pipeline 执行顺序忽略性能与依赖（架构类），并附插件可读素材
> - 2026-09-08：收录 #5 df.empty 混淆行空与列空（API 语义类），插件素材补 PITFALL-005
> - 2026-09-08：收录 #6 教学代码片段默认变量已定义（AI 教学产物类），插件素材补 PITFALL-006
> - 2026-09-10：收录 #7 架构文档漏掉数据读取层（架构/契约类），插件素材补 PITFALL-007；
>   同步架构升 v1.2（补 `data/loader.py`、§5.1 线性数据流、loader 输出契约）
> - 2026-09-14：收录 #8 假绿测试（测试类：参数不触发被测路径，短路掩盖未执行代码），
>   插件素材补 PITFALL-008；同步修 `data/loader.py` 冒烟块为"三类分支"覆盖
> - 2026-09-16：收录 #9 契约字段只写在文档里（契约类 · AI 产物：架构文档写了 columns_added，
>   但 base.new_step_report 模板没预置 → 漏填/拼错静默通过），插件素材补 PITFALL-009
> - 2026-09-16：收录 #10 测试只喂"正常数据"（测试类 · AI 产物：脏数据一测就抓到
>   check_new_columns 用错 / dropna().sum() 应为 isna().sum()），插件素材补 PITFALL-010
